其中，LL 子带表示低频近似成分，主要保留图像的整体结构和主要轮廓；LH、HL 和 HH 子带分别表示不同方向上的高频细节成分，对边缘、纹理及局部变化更加敏感。需要说明的是，Cognition Net 主网络对四子带离散小波变换（Discrete Wavelet Transform, DWT）及逆离散小波变换（Inverse Discrete Wavelet Transform, IDWT）重建进行了改进：首先对每个输入通道独立施加固定的 2×2 混合正负权核 K=[[1,1],[1,-1]]，步长为 2，使每个输入通道生成一张局部响应图；随后通过 1×1 卷积和 ReLU 将响应映射为 C_m=max(8,C/16) 个中间通道，并据此生成通道与空间注意力。为与标准 LL、LH、HL 和 HH 四子带 Haar 变换区分，该文将该实现称为改进型 Haar 局部滤波注意力，以区别于标准四子带Haar变换。
该文将改进型 Haar 局部滤波引入注意力模块中，用于增强浅层特征中的边缘和纹理响应，该文将该实现称为小波注意力机制。WA模块结构如图2 所示。
该模块采用固定的 2×2 混合正负权核对输入特征进行逐通道滤波，并以步长为 2 完成空间下采样。由于每个输入通道仅生成一张响应图，因此输出通道数仍为 C，得到的响应同时包含局部聚合信息与结构差异信息。
作为对照，标准四子带 Haar 变体分别使用 LL、LH、HL 和 HH 四个固定滤波核，使每个输入通道生成四张子带响应图，并沿通道维拼接为 4C 个通道后再通过 1×1 卷积压缩至相同的中间通道数。